'use strict';

describe('myApp.error module', function() {

  beforeEach(module('myApp.error'));

  describe('error controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view3Ctrl = $controller('View3Ctrl');
      expect(view3Ctrl).toBeDefined();
    }));

  });
});